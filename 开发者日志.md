## 人机对话平台开发日志 



1.判别器的输入问题

第一想法是将用户问题 + 模型特征输入，但任务书只输入"模型特征",是因为判别器太小不具备很好的语言理解能力，所以判断问题难易这一步就完全交给DST模型实现了?
另外"模型特征这一feature"是什么?是各个slot和value的预测置信度吗? 那判别器模型的input该怎么设计呢??（不同任务的格式化样式可能不一样，有的slot比较多有的比较少，要做padding统一格式还是?）

初步设想（先暂时这么跑起来，回头根据实际的判别器在修改输入即可）:

$$X = [f_1, f_2, f_3, f_4, f_5]$$

- $f_1$ (**模型自信度**): DST 预测当前 Slot 的平均概率值 (e.g., 0.85)
- $f_2$ (**模型混乱度**): 预测分布的熵 (e.g., 0.2)
- $f_3$ (**问题长度**): 用户这就话有几个词 (e.g., 15)
- $f_4$ (**对话轮次**): 当前是第几轮 (e.g., 3)
- $f_5$​​ (**意图类型**): 这是一个查询意图还是预订意图 (e.g., One-hot encoding)

----------------------------------------------------------------------------------------------

1/20 

①完成了前后端的API互通，另外一些小细节还没做(Setting/用户面板/登录cookie设置/状态同步等等),但是基本的专家修改slot和用户对话基本实现。

----------------------------------------------------------------------------------------------

1/22

①DST模型的brief state 始终是空的; Policy Model 容易结束对话。

②专家会话Session_id传递及判定问题(暂时解决)

③Session_id的绑定问题:绑定会话而非绑定窗口



④**另外，生成式DST(T5)的breif state生成是完全依靠history的，修改slots只会修改这一次会话生成，后续的会话由于history不变依旧会保持原本的生成**
For example：

User :  一个极其复杂的句子A[表面看上去是接收中国菜,实际用户意图是想要AI介绍日本菜]

Expert : 
          {food : ~~chinese~~ japan} (专家识别到用户实际是想要介绍日本菜，于是修改了slot)

Agent :  What kind of food do you like?

User : Noodles is ok.

Expert :

​	 {food type : noodles (√)}

**此时DST看到的对话history是['User:A' : 'Agent: What kind of food do you like?','User:Noodles is ok']**
**原本DST就无法正确理解A，专家修正slot后，DST在后续依旧无法正确理解A[因为在这个情境下修改A并没有对history产生什么影响],所以是不是就可能导致Agent在后续的对话中依旧无法正确理解用户意图?**

**Agent: Here are some Chinese Noodles restrauant.(×)**

**可能的解决方法: 在专家修改完slot之后同步修改一下history??但这一步要怎么做?直接做单词检索然后替换吗??或者有没有什么模型，可以根据slot生成问题，然后用这个生成的问题去替代原本用户提问??(类似NLG模型可以根据关键词生成自然语言回答)**



⑤实现了修改后信息的本地保存,数据格式为:

~~User:text~~
~~DST_BriefSlots (原始生成的slots)~~
~~Expert_Slots (专家修改后的slots)~~
~~这里没有做区分，二者可能一致(专家未修改)~~

数据格式暂时不确定，需要根据实际的DST来决定。如果是生成式DST，那么需要保存的就是前面完整的对话历史[input]+专家修改后的本次slot[output]。如果是基于其他形式的DST(每了解过还有什么形式的DST，回头调研一下),输入格式可能需要修改，这里暂时不存了(┬＿┬)

⑥将源码上传到github仓库(√)

⑦关于评价指标

​	人机对话系统的评价指标，常规来讲(MultiWOZ)一般从两个角度考虑，一是Inform Rate(提供信息率),二是Success Rate(任务成功率)。

​	其中一关注是否推荐了符合所有约束条件的实体，通过系统提供的实体ID是否在数据库的**"正确答案合集"**里进行判断。

​	举例来说，如果用户希望系统推荐性价比高的中餐，恰好答案集合中有这样的餐厅，如果系统正确向用户推荐了这个餐厅，则Inform = 1;如果系统推荐了另外一个昂贵的餐厅，或者直接说找不到，那么就是Inform=0;

​	第二个指标关注的更为严格，除了Inform正确以外，还要求Book正确/Request正确,但是我们的Agent真的能涉及真实的Book需求吗??暂时没考虑去做这方面的实现，所以先不考虑。

​	另外还有一个叫Joint Goal Accuracy的东西,也是通用性指标。

​	**但是！上面这些指标基本都是用在有监督数据集上的东西，用于模型性能，并不适用于真实的交互(因为没有一个标准的答案用于计算指标。)为了证明专家介入优于传统方法，目前的一些考虑如下:**
​	**收集用户反馈，在每次Agent输出Bye结束任务会话后都在前端给用户一个选择的空间来评价这次任务是否正确完成(点赞 or 点踩 or 未操作 or 继续提问[一定程度上可以代表这次任务并没有圆满完成])。记录这些指标**

​	**然后去做对比实验，在一定时间点内不使用专家介入(即传统的任务型对话Agent)，记录用户满意度;一定时间内使用智能判断专家介入的功能并记录用户反馈，通过用户满意占比来证明专家介入的有效性。**

​	==缺点：需要很多很多的对话来佐证这一结论==

​	**此外，如果要证明模型可以被收集到的专家Answer进一步调优，可以尝试下面的方法:引入一个干预率(Intervention Rate)的概念，其代表在一轮对话中专家干预的占比**

​	**Intervention rate = 专家修改的轮次/总对话轮次**

​        **这个指标越低，证明AI模型越强（理论上来讲，随着数据的不断收集，这个指标会越来越低）**

​	~~再补充一点，还可以统计一下专家修改的比例用于评判判别器的强弱。如果判别器认为需要专家修改但实际上专家没有修改slot这是否说明判别器的功能不够强，没能识别真实的困难问题?[也不好说，因为初衷是将“不确定”的问题交给专家修改，所以有些不需要修改的也是正常的?]~~

----------------------------------------------------------------------------------------------
1/24

① 完成了用户登陆状态的保存以及检测逻辑,完善了本地的数据库，基本实现了注册功能以及密码/token加密

② 优化了后端的代码结构。早期代码都集中放在main.py当中，随着代码越来越多，代码越来越臃肿，于是将代码分成了多个文件。顺手规范了一下密钥这些环境变量放的位置[🍬]



~~(哎我c这后端怎么这么麻烦T_T)~~

-----------------------------------------------------------------------------------------------
1/26
DeBug ing (T_T) 把代码塞到不同文件夹下虽然简洁了很多但是Debug变得好麻烦，要不停切换不同文件。

**!!! 我真的生气了 !!! 到底是谁写的bcrypt这个库！ 乱报错**
  File "D:\anaconda3\envs\HAChat\lib\site-packages\passlib\handlers\bcrypt.py", line 655, in _calc_checksum
    hash = _bcrypt.hashpw(secret, config)
**ValueError: password cannot be longer than 72 bytes, truncate manually if necessary (e.g. my_password[:72])**
**这个报错是因为版本不兼容，我D了老半天结果你告诉我是你的版本问题??你在raise什么东西??**

同步异步的问题,没怎么搞懂,之前异步登录报错了,现在改成同步，回头学懂了再回来看看。

完成了会话相关数据库结构的设计，下一步要去做会话session绑定[目前是和页面绑定，而不是和具体的会话绑定，因此暂时无法查看会话历史]

----------------------------------------------------------------------------------------------

# TODO
1 . Session_id 的设置 : 目前是绑页面而非绑会话。用户从一个会话切到另一个会话会生成一个新的会话id,但是不会保存原本的会话id。用户如果再次切回去之前的会话会导致会话丢失。

2. 会话历史实际上还没有做，这个不适合保存在前端，计划是为每一个会话分配一个永久id,后端根据id记录，前端根据id访问[前端需要先得到一个用户id,然后通过用户id去获取会话id]。

~~ 3. 登录Cookie设置，目前的登录模块还是摆设。~~

4. 个人信息页面等琐碎的小功能。

5. 完整会话的保存(用于后续优化模型)

6. 记录用户对AI回答的满意度(点赞/点踩),用于评价模型性能。

7. 目前的Policy模型有一点点的问题[加载的参数不一致，可能导致policy模型输出异常，大概就是因为这个policy模型显得傻不啦叽的]，暂时没时间去管它，回头做完5.6之后去看一下这部分。
---------------------------------------------------------------------------------------------
RvLen  1/22/2026 于北京
